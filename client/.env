# For local development, leave this empty to use Vite's dev server proxy.
# This prevents CORS and cookie issues on different ports (5173 vs 5000).
# In production, set this to your live backend API server URL (e.g., https://api.yourdomain.com).
VITE_BACKEND_URL=https://on-demand-service-platform-uwzb.vercel.app
